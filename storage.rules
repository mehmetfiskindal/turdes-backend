rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read or write to any path (for emulator testing)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
